[package]
name = "easy-acap-events"
version = "0.1.0"
edition.workspace = true

[dependencies]
anyhow = { workspace = true }
axevent = { workspace = true, optional = true }
chrono = { workspace = true, features = ["serde"] }
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
serde_json.workspace = true

[features]
target = ["axevent"]
