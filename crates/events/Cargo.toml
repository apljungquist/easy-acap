[package]
name = "easy-acap"
version = "0.1.0"
edition.workspace = true

[dependencies]
anyhow = { workspace = true }
axevent = { workspace = true }
glib = { workspace = true }
log = { workspace = true }
tokio = { workspace = true, features = ["sync"] }

[dev-dependencies]
tokio = { workspace = true, features = ["test-util", "time", "rt", "rt-multi-thread", "macros"] }
